# リファクタリング計画書

作成日: 2026-06-11

このリポジトリ（単一HTMLファイル形式のWebアプリ集、計約7,900行）に蓄積した重複・デッドコード・手作業依存を、フェーズに分けて段階的に解消するための計画書。

---

## 1. 現状分析サマリー

### 1.1 コードベースの構成

| 分類 | ファイル | 行数 | 状態 |
|---|---|---|---|
| ポータル | `index.html` | 512 | 稼働中（作品カードがハードコード） |
| TR-808系 | `tr808.html` / `tr808-v2.html` / `tr808kids.html` | 計1,840 | 3ファイルとも公開中・大量重複 |
| 楽器・遊び | `grid.html` / `fireworks.html` / `otamatone.html` / `project/` | 計約2,200 | 稼働中 |
| ゲーム | `virus-busters.html` | 1,380 | 稼働中 |
| 情報系 | `news.html` | 763 | 稼働中 |
| PWAデモ | `memo.html` + `sw.js` + `manifest.json` + `icons/` | 計約1,270 | index未掲載（READMEでPWAデモとして言及） |
| **孤立** | `connection-os.html` | 808 | **どこからもリンクされていない** |
| ツール | `generate_icons.py` | 93 | 手動実行のみ |
| 記録 | `logs/` | — | 作業日誌（意図的にトラッキング） |

### 1.2 特定された無駄（定量）

1. **TR-808ファミリーの重複: 約550〜680行（同系3ファイルの30〜37%）**
   - AudioContext初期化・エフェクトバス（リバーブ/ディレイ/コーラス/トレモロ）・マスターコンプレッサー設定が3ファイルでほぼ同一
   - ノブ描画関数 `drawKnob()` がピクセル単位で同一
   - 鍵盤シンセ（`createVoice`、音階配列、タッチ処理）が同一
   - → **音のバグを直すたびに3ファイル修正が必要**な状態

2. **全ファイル共通のボイラープレート: 約930行（コードベースの約12%）**
   - CSSリセット（`box-sizing`/`tap-highlight`/`touch-action`/`overscroll-behavior`等）が11ファイルに各50行前後
   - iOS向けハック（`safe-area-inset`、`100dvh`、バウンス抑止）が6ファイル以上に散在
   - AudioContextアンロック処理（touchstart/clickで `resume()`）が音系5ファイルに各15〜20行
   - カラーパレット（`--paper`/`--ink`/`--terracotta`）が5ファイルで重複定義

3. **デッドコード・整合性の欠如**
   - `connection-os.html`（808行）が完全に孤立
   - `memo.html` は index.html にも README の収録作品表にも未掲載
   - README の収録作品表（6作品）と index.html の掲載作品（9作品）が**不一致**
   - `.gitignore` がNode.js用ボイラープレート139行（本プロジェクトにnpm/webpackは無い）

4. **手作業依存**
   - 新作品の追加に index.html と README.md の2箇所をハードコード編集
   - `sw.js` のキャッシュバージョン（`memo-v2`）を手動でbump
   - アイコン再生成は `generate_icons.py` の手動実行
   - リント・テスト・CI・デプロイ検証が一切無し（GitHub Pagesに直接公開）

5. **コード品質**
   - オーディオパラメータのマジックナンバー20箇所以上（コンプレッサー閾値、コーラスLFO周波数等）に名前が無い
   - 変数命名の不統一（`audioCtx` vs `actx`）、イベントハンドラの書き方混在

---

## 2. 設計方針（リファクタリングの前提）

リファクタリングに入る前に、このリポジトリの「守るべき性質」を明文化する。

- **「1作品=1HTMLファイル」は基本的に維持する。**
  単一ファイルであることは、コピーして配れる・ブラウザで直接開ける・GitHub Pagesでビルド不要、という本リポジトリ最大の利点。共通化は「外部ファイルへの抽出」と「ファイル内の整理」のバランスを取り、ビルドツール（webpack等）は導入しない。
- **iPhone Safari実機での動作が最優先。** 自動テストが無い分、各フェーズに手動検証手順を必ず含める。
- **公開URLを壊さない。** 既存ファイル名（= 公開URL）は原則変更しない。削除する場合もリダイレクトか告知を検討する。
- **1フェーズ=1PR。** 各フェーズは独立してマージ・検証・ロールバックできる粒度に保つ。

### 共通化の方式（フェーズ2・3で使用）

GitHub Pages上で動くため、共通コードは**ビルド無しで読み込める静的ファイル**として抽出する：

```
shared/
  base.css        … CSSリセット + iOSハック + safe-area
  theme.css       … --paper/--ink 等の共通パレット（テーマ別に分割可）
  audio-unlock.js … AudioContext生成＆アンロックのユーティリティ
  synth-core.js   … TR-808系共通エンジン（フェーズ3）
```

各HTMLは `<link>` / `<script src>` で読み込む。「完全な単一ファイル性」は失われるが、同一オリジン内の2〜3ファイル参照に留め、オフラインコピー配布が必要な作品は例外として単一ファイルのまま残してよい。

---

## 3. フェーズ別計画

### フェーズ0: 安全網の整備（リファクタ前の準備）

**目的**: 「壊れていないこと」を確認する手段を先に作る。コードは変更しない。

| # | 作業 | 詳細 |
|---|---|---|
| 0-1 | 動作確認チェックリスト作成 | `docs/smoke-test.md` を新規作成。全9公開作品＋memo.htmlについて「開く→主要操作1つ→音が鳴る/描画される」の確認項目を列挙。iPhone Safari と PCブラウザの2列。 |
| 0-2 | ベースライン記録 | 現状の全作品をローカルサーバ（`python3 -m http.server`）で開き、チェックリストを一巡して結果を記録。既存バグがあればこの時点で「既知の問題」として記載（リファクタのスコープ外と明確化）。 |
| 0-3 | リンク切れ検査 | index.html / README.md 内の全リンクが200を返すか確認（簡単なスクリプトで可）。 |

- **成果物**: `docs/smoke-test.md`（以後の全フェーズの検証基準）
- **目安**: 1〜2時間
- **リスク**: なし（コード変更ゼロ）

---

### フェーズ1: デッドコード整理とドキュメント整合（即効・低リスク）

**目的**: 「何が生きているか」を確定させ、以後のフェーズの対象範囲を最小化する。

| # | 作業 | 詳細 |
|---|---|---|
| 1-1 | `connection-os.html` の処遇決定 | 808行が完全孤立。**要オーナー判断**: (a) 削除（git履歴に残るので復元可能）、(b) 完成させて index.html に掲載、(c) `archive/` ディレクトリへ移動して「保管」を明示。推奨は (a) または (c)。 |
| 1-2 | `memo.html` の位置づけ明確化 | PWAデモとして残すなら index.html に「実験室」セクションを設けて掲載するか、README収録作品表に「実験的」として追記。非公開の開発用なら README にその旨を明記。 |
| 1-3 | README と index.html の同期 | README の収録作品表（現在6作品）を index.html の9作品に合わせて更新。※フェーズ4でデータ駆動化するまでの暫定措置。 |
| 1-4 | `.gitignore` の簡素化 | Node.js用139行を、本プロジェクトに実際に必要な数行（`.DS_Store`、`__pycache__/`、エディタ設定等）に置換。 |
| 1-5 | `CLAUDE.md` 新規作成 | リポジトリの構成・設計方針（§2の内容）・「新作品追加の手順」「sw.jsのキャッシュbumpルール」を記載。AI支援開発時の重複再発防止にも効く。 |

- **検証**: smoke-test全項目 + リンク切れ検査
- **目安**: 2〜3時間
- **リスク**: 低。1-1の削除のみ要確認（公開URLを踏んでいる人がいる可能性は低いが、削除前にアクセス手段の有無をオーナーに確認）

---

### フェーズ2: 共通ボイラープレートの抽出（約930行 → 約150行）

**目的**: 11ファイルにコピペされたCSSリセット・iOSハック・オーディオアンロックを共通ファイル化し、修正箇所を1箇所にする。

| # | 作業 | 詳細 |
|---|---|---|
| 2-1 | `shared/base.css` 作成 | CSSリセット（`box-sizing`/`margin`/`padding`）、`-webkit-tap-highlight-color`、`touch-action`、`user-select`、`overscroll-behavior`、`safe-area-inset` パディング用ユーティリティクラス、`100dvh` 対応を集約。 |
| 2-2 | `shared/theme.css` 作成 | `--paper`/`--ink`/`--terracotta` 系パレット（index/news/otamatone/virus-busters/projectで共用）と、フォントスタック（"Hiragino Kaku Gothic ProN" の表記ゆれ5種を1つに統一）を集約。memo/connection-os系の `--bg`/`--accent` パレットは使用ファイルが残る場合のみ第2テーマとして併設。 |
| 2-3 | `shared/audio-unlock.js` 作成 | `getAudioContext()`（生成＋suspended時resume＋touchstart/clickアンロック登録）を1関数に。grid/otamatone/tr808系5ファイルの各15〜20行を置換。あわせて変数名を `audioCtx` に統一。 |
| 2-4 | 各HTMLへの適用 | **1ファイルずつ**置換→smoke-test→コミット、の順で進める（11ファイル一括変更は事故時の切り分けが困難）。適用順は影響の小さい順: fireworks → grid → news → index → otamatone → tr808系 → virus-busters → memo → project。 |
| 2-5 | sw.js キャッシュリスト更新 | memo.html が shared/ を参照するようになった場合、`ASSETS` に追加し `CACHE_NAME` をbump。 |

- **検証**: 各ファイル適用ごとにsmoke-test該当項目。特にiPhone実機で safe-area・スクロールバウンス・音のアンロックを確認
- **目安**: 4〜6時間（適用が11ファイルに分かれるため）
- **削減見込み**: 約930行 → 共通ファイル約150行（**正味 約780行削減**）
- **リスク**: 中。CSSリセットの微妙な差異（ファイルごとに `touch-action: manipulation` と `none` の違い等）があるため、機械的な一括置換はせず、差異は各HTML側に残す。**「単一ファイルで配布したい作品」があればこのフェーズから除外する**（要オーナー確認、§2参照）

---

### フェーズ3: TR-808ファミリーの統合（約600行削減・本丸）

**目的**: 3ファイルに重複したオーディオエンジンを単一ソース化し、「音のバグ修正=1箇所」にする。

| # | 作業 | 詳細 |
|---|---|---|
| 3-1 | 差分の精密マッピング | tr808.html（素の3音色版）/ tr808-v2.html（+ルーパー183行）/ tr808kids.html（+ゲームモード130行）の共通部と差分を関数単位で表にする。`drawKnob()` は同一確認済みだが、エフェクト周りは「ほぼ同一」なので diff で完全一致/微差を仕分けする。微差が**意図的な調整**（音作りの違い）か**修正漏れ**かをコミット履歴から判定。 |
| 3-2 | `shared/synth-core.js` 抽出 | 共通部を抽出: AudioContext/エフェクトバス構築（`buildIR`、リバーブ/ディレイ/コーラス/トレモロ）、マスターコンプレッサー、`drawKnob()` とノブ状態管理、`createVoice()` と音階定数（`WHITE_NOTES`/`BLACK_POSITIONS`）、鍵盤のポインタイベント処理。 |
| 3-3 | マジックナンバーの定数化 | 抽出と同時に `SynthConfig` オブジェクトへ集約: コンプレッサー設定（-12dB/ratio 8/attack 0.003/release 0.25）、コーラスLFO 0.5Hz、ディレイベース 0.05+0.30*0.95、トレモロ深度カーブ等。バリアントごとの音作りの違いは config の上書きで表現。 |
| 3-4 | 3バリアントの書き換え | 各HTMLは「UIマークアップ + 固有機能（ルーパー/ゲームモード） + synth-core呼び出し」のみに。1ファイルずつ: tr808.html（最小）→ tr808-v2.html → tr808kids.html の順。 |
| 3-5 | 統合検証 | 3作品の音色が**リファクタ前と聴感で同一**であることを確認（可能なら前後でループ録音して比較）。kids のゲーム判定、v2 のルーパー録音/再生/ピッチ変更を重点確認。 |

- **検証**: smoke-test + 上記の聴感比較・機能別重点確認（iPhone実機必須）
- **目安**: 6〜10時間
- **削減見込み**: 約550〜680行 → 共通モジュール約250行（**正味 約350〜430行削減**）
- **リスク**: 高め。Web Audioはタイミング依存のバグが出やすく、自動テスト不在のため検証は耳と手に頼る。**このフェーズだけは3コミット（バリアントごと）以上に分割**し、1バリアントずつマージ可能にしておく

---

### フェーズ4: 作品レジストリのデータ駆動化

**目的**: 「新作品の追加=1箇所の編集」にし、index.html と README の不整合を構造的に再発不能にする。

| # | 作業 | 詳細 |
|---|---|---|
| 4-1 | `works.json` 作成 | 全作品のメタデータ（ファイル名/番号/タイトル/キャプション/タグ/公開状態 `public`・`lab`・`archived`）を1ファイルに集約。 |
| 4-2 | index.html のカード生成をJS化 | ポータルページが `works.json` を fetch して作品カードを描画する方式に変更（GitHub Pagesではfetchが同一オリジンで動く）。JS無効環境向けに `<noscript>` で簡易リスト併記。 |
| 4-3 | README生成スクリプト | `tools/update_readme.py`（generate_icons.py と同じPython系で統一）が works.json から README の収録作品表を再生成。手動実行でよい（CIはフェーズ6）。 |
| 4-4 | 新作品追加手順の更新 | CLAUDE.md / README の手順を「works.json に1エントリ追加 → スクリプト実行」に書き換え。 |

- **検証**: index.html の表示が現状と一致すること（カード9枚・番号・リンク先）、README表とworks.jsonの一致
- **目安**: 3〜4時間
- **リスク**: 低〜中。fetch失敗時のフォールバック表示だけ丁寧に。**代替案**: JS生成を嫌うなら「works.json → index.html を再生成するスクリプト」方式（静的なまま）でもよい。表示の確実性ではこちらが上（要オーナー好み確認だが、推奨は静的生成方式）

---

### フェーズ5（任意）: PWA基盤の一般化

**目的**: memo.html 専用のPWA構成を、他作品にも展開可能な形に整える。**ニーズが無ければ実施しない。**

| # | 作業 | 詳細 |
|---|---|---|
| 5-1 | sw.js のキャッシュリスト自動化 | works.json（フェーズ4）から `ASSETS` 配列を生成するスクリプトを用意し、手動bump忘れを防ぐ（キャッシュ名にコンテンツハッシュか日付を含める）。 |
| 5-2 | オフライン化したい作品の選定 | 全作品PWA化は過剰。オフラインで使いたい実需があるもの（例: 子ども向けのtr808kids、virus-busters）だけ選定。 |
| 5-3 | manifest の複数アプリ対応 | 作品ごとに manifest を分けるか、共通manifest + `start_url` 切替か方式を決定して実装。 |

- **目安**: 4〜8時間（対象数による）
- **リスク**: 中。Service Workerのキャッシュ事故は「更新が反映されない」という形で現れ気づきにくい。フェーズ5-1（自動化）を先にやってから対象を増やすこと

---

### フェーズ6（任意）: 品質基盤（CI・リント）

**目的**: 再発防止の仕組み化。手動運用で困っていなければ最小限でよい。

| # | 作業 | 詳細 |
|---|---|---|
| 6-1 | GitHub Actions: リンクチェック | push時に index.html / README.md のリンク切れと、works.json と実ファイルの突合を検査（フェーズ0-3・4-3のスクリプトを流用）。 |
| 6-2 | HTML/JS の静的検査 | `htmlhint` + `eslint`（設定は緩めに、ブラウザグローバル前提）。既存コードを通すための初期修正は機械的な範囲に留める。 |
| 6-3 | README生成の自動化 | works.json 変更時に README 再生成をCIで検証（生成結果と差分があれば失敗）。 |

- **目安**: 2〜4時間
- **リスク**: 低。ただしCIを増やしすぎると趣味プロジェクトの軽快さが失われるため、「リンクチェック＋レジストリ突合」だけでも十分

---

## 4. やらないこと（明示的なスコープ外）

過剰設計を避けるため、以下は**実施しない**と明記する：

- **TypeScript移行**（20時間超の工数に対し、この規模・用途では見合わない）
- **バンドラ/ミニファイア導入**（GitHub Pagesのビルド不要性という利点を損なう。合計300KB弱で性能問題も無い）
- **フレームワーク導入**（React等。単一HTML文化と相反する）
- **テストフレームワーク導入**（Web Audio・Canvas・タッチ操作が主体で自動テストの費用対効果が低い。smoke-test.md の手動検証で代替）
- **logs/ の整理**（作業日誌は意図的な運用。触らない）
- **既存URLの変更・リネーム**（公開済みリンクを壊さない）

---

## 5. 全体スケジュールとマイルストーン

| フェーズ | 内容 | 目安工数 | 削減効果 | リスク | 依存 |
|---|---|---|---|---|---|
| 0 | 安全網（smoke-test） | 1〜2h | — | なし | — |
| 1 | デッドコード整理・文書整合 | 2〜3h | 最大808行 | 低 | 0 |
| 2 | 共通ボイラープレート抽出 | 4〜6h | 約780行 | 中 | 0 |
| 3 | TR-808エンジン統合 | 6〜10h | 約350〜430行 | 高 | 0, 2 |
| 4 | 作品レジストリのデータ駆動化 | 3〜4h | 不整合の構造的解消 | 低〜中 | 1 |
| 5 | PWA一般化（任意） | 4〜8h | 運用事故防止 | 中 | 4 |
| 6 | CI・リント（任意） | 2〜4h | 再発防止 | 低 | 4 |

- **必須パス**: フェーズ0 → 1 → 2 → 3 → 4（計 16〜25時間）。フェーズ5・6は必要になってから。
- **行数効果**: 約7,900行 → 約6,000行前後（**2割強の削減**）＋ 修正箇所の一元化（質的効果のほうが大きい）
- 各フェーズは1PRとし、マージ前に必ず smoke-test を一巡する。フェーズ2と3は内部でさらにコミットを分割する（§3参照）。

## 6. 着手前にオーナーへ確認すべきこと

1. `connection-os.html` を削除してよいか（削除/アーカイブ/完成させて公開）— フェーズ1-1
2. `memo.html` をポータルに掲載するか — フェーズ1-2
3. 「単一HTMLファイルのまま残したい（配布したい）作品」はあるか — フェーズ2の除外対象決定
4. index.html はJS動的生成と静的再生成のどちらが好みか（推奨: 静的再生成）— フェーズ4-2
5. PWA化したい作品は実際にあるか — フェーズ5の実施判断
